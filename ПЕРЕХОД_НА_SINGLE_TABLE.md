# 🔄 Переход на Single Table Schema (v3)

Мы перешли на **упрощенную и более мощную** схему с одной таблицей `analytics`.

---

## 🚀 Почему это лучше?
1. **Простота:** Больше нет `users` + `sessions` + `events`. Всё лежит в одной таблице.
2. **Скорость:** Скрипт делает **в 3 раза меньше запросов** (не нужно создавать юзера и сессию перед отправкой событий).
3. **Аналитика:** SQL аналитика стала проще. Не нужно делать `JOIN`. Вся информация (UTM, устройство, URL) есть в КАЖДОЙ строке события.

---

## 🛠 Что сделать прямо сейчас (3 шага):

### 1. Создайте таблицу в Supabase
1.1. Откройте файл `database/schema_single_table.sql`.
1.2. Скопируйте код и выполните в **SQL Editor** Supabase.
1.3. Теперь у вас есть таблица `analytics`.

### 2. Обновите код в Tilda
2.1. Откройте `НОВАЯ_CXEMA_СБОРА.html`.
2.2. Скопируйте **ВЕСЬ** код.
2.3. Вставьте в **Tilda → Site Settings → Advanced → Code in <head>**.
2.4. (Старый код удалите).

### 3. Опубликуйте сайт
3.1. Save → Publish.
3.2. Проверьте консоль браузера: должно быть `[TildaAnalytics] session_start`.

---

## 📊 Как теперь работает аналитика?

Все события (просмотры, клики, формы) отправляются в таблицу `analytics`.

**Пример записи в БД:**
- `event_name`: 'page_view'
- `user_id`: 'abc-123'
- `data`: {
    "utm_source": "yandex",
    "is_mobile": true,
    "page_title": "Главная",
    ...и так далее
  }

Это позволяет фильтровать данные как угодно, не заботясь о связях таблиц.
