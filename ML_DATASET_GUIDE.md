# ML Dataset Export - Quick Start

## Что это?
Функция автоматической подготовки данных для обучения ML модели в Vertex AI Studio.

## Шаг 1: Создать таблицу в Supabase

Выполните SQL скрипт `database/4_create_ml_dataset.sql` в Supabase SQL Editor:

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. Перейдите в SQL Editor
4. Скопируйте содержимое файла `4_create_ml_dataset.sql`
5. Нажмите "Run"

## Шаг 2: Использование

1. Откройте дашборд
2. Выберите нужный период и кампании (фильтры)
3. Нажмите кнопку **"🚀 Подготовить DS для ML"**
4. Дождитесь сообщения "✅ Успешно!"

## Что происходит?

Скрипт:
- Берет все отфильтрованные данные из таблицы `analytics`
- Разворачивает JSONB поле `data` в плоские колонки
- Сохраняет в таблицу `ml_dataset` с правильными типами данных
- Использует `upsert` (не создает дубликаты)

## Экспорт в Vertex AI

После подготовки данных:

1. Откройте Supabase Table Editor
2. Выберите таблицу `ml_dataset`
3. Экспортируйте как CSV (кнопка Export)
4. Загрузите CSV в Vertex AI Studio

## Целевая переменная

`target_is_lead` - предсказываем конверсию (станет ли пользователь лидом).

## Фичи (Features)

- Engagement: время, скролл, скорость мыши
- Behavior: rage clicks, переключения вкладок
- Device: OS, RAM, CPU, разрешение
- Traffic: источник, кампания
