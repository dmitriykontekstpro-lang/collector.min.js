# 🧪 Как протестировать Tilda Analytics (v3)

## ✅ Этап 1: Проверка в браузере

1.  **Откройте ваш сайт** в режиме инкогнито (чтобы сбросить кэш).
2.  **Откройте консоль разработчика** (F12).
3.  **Проверьте инициализацию:**
    В консоли должно быть сообщение: 
    > `✅ Tilda Analytics v3 initialized`
    
    *Если его нет — скрипт не загрузился или не вставлен в HEAD.*

4.  **Проверьте отправку событий:**
    Если в настройках `debug: true`, вы увидите:
    > `[TildaAnalytics] page_view`
    > `Sent 1 events`

## 📡 Этап 2: Проверка сети (Network)

1.  Перейдите во вкладку **Network** (Сеть) в F12.
2.  В фильтре напишите: `analytics`.
3.  Обновите страницу.
4.  Найдите запрос к `...supabase.co.../analytics`.
5.  **Статус должен быть 201**.
    *   🔴 `404` — Неверный URL Supabase.
    *   🔴 `401` — Неверный API Key.
    *   🔴 `400` — Таблица `analytics` не создана в базе.
    *   🔴 `429` — Слишком много запросов (редко).

## 🗄 Этап 3: Проверка в базе данных

1.  Зайдите в **Supabase Dashboard**.
2.  **Project:** `qqfyjrugrinmdijpsutj`.
3.  **Table Editor** -> таблица `analytics`.
4.  Вы должны увидеть новые строки.

**Что проверить внутри строки:**
*   `event_name`: 'page_view'
*   `user_id`: не пустой
*   `data`: нажмите на ячейку, там должен быть JSON:
    ```json
    {
      "ua": "Mozilla/...",
      "screen": "1920x1080",
      "is_mobile": false,
      "page_title": "Главная"
    }
    ```

## 🛠 Ручной тест (Вызов из консоли)

Вставьте эту команду прямо в консоль браузера на вашем сайте, чтобы проверить запись:

```javascript
// Тестовое событие
TildaAnalytics._trackEvent('test_manual_event', { text: 'Hello Supabase!' });

// Принудительная отправка (сброс буфера)
TildaAnalytics._flush();
```

Если после этого в Supabase появилась запись с `event_name` = `test_manual_event` — **система работает на 100%**.
